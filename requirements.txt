# Minimal dependency set for the NYC crash analysis.
# No ML libraries: this is an EDA / feature-engineering / visualization project.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
requests>=2.28
pyarrow>=12.0        # Parquet engine for the processed dataset
